package mukkeu.mukkeu.post.dto;

/**
 * 좋아요/취소 공통 응답. 요청 후의 상태를 그대로 돌려준다.
 *
 * 이미 누른 상태에서 또 눌러도 200 이고, 안 누른 상태에서 취소해도 200 이다.
 * 좋아요는 "눌렀다" 는 사건이 아니라 "눌려 있다" 는 상태이기 때문이다.
 * 상태를 원하는 값으로 맞춰 달라는 요청이므로, 이미 그 상태면 할 일이 없을 뿐 실패가 아니다.
 * 409 를 내면 프론트는 실패로 처리해 화면을 되돌리는데, 사용자가 보기엔 멀쩡히
 * 눌린 하트가 풀리는 셈이다.
 */
public record LikeResponse(
	int likeCount,
	boolean liked
) {
}
